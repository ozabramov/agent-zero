[build]
dockerfilePath = "docker/run/Dockerfile"
dockerContext = "docker/run"

[build.args]
BRANCH = "main"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
